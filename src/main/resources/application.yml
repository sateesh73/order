spring:
  application:
    name: order
  datasource:
    url: jdbc:mysql://localhost:3306/orderDB
    username: root
    password: admin
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8084
